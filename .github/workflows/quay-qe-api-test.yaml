# Workflow to run Quay QE API tests with Cypress
name: Quay QE API Tests
on:
  pull_request:
    branches:
      - "*"
    paths:
      - "endpoints/**"
      - ".github/actions/setup-quay/**"
      - ".github/workflows/quay-qe-api-test.yaml"
  issue_comment:
    types: [created]

jobs:
  cypress:
    name: Quay QE API Tests
    runs-on: ubuntu-22.04
    # Only run on PR comments containing "/quay-qe-api-test" or on pull_request events
    if: |
      (github.event_name == 'pull_request') ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request &&
       contains(github.event.comment.body, '/quay-qe-api-test') &&
       (github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'OWNER' ||
        github.event.comment.author_association == 'COLLABORATOR'))
    steps:
      - name: React to comment
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'rocket'
            })

      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          # For issue_comment trigger, checkout the PR head ref
          ref: ${{ github.event_name == 'issue_comment' && format('refs/pull/{0}/head', github.event.issue.number) || '' }}

      - name: Setup Quay
        id: setup-quay
        uses: ./.github/actions/setup-quay
        with:
          extra-config: |
            FEATURE_USER_INITIALIZE: true
            FEATURE_USER_METADATA: false
            FEATURE_MAILING: false
            FEATURE_SUPERUSER_CONFIGDUMP: true
            PERMANENTLY_DELETE_TAGS: true
            CREATE_NAMESPACE_ON_PUSH: true
            FEATURE_QUOTA_MANAGEMENT: true
            FEATURE_PROXY_CACHE: true
            SERVER_HOSTNAME: localhost:8080
            # Remove OIDC config that points to non-existent Keycloak in GHA
            SOMEOIDC_LOGIN_CONFIG: null
            # Fix CORS to allow requests from localhost:8080
            CORS_ORIGIN:
              - "http://localhost:8080"
              - "http://localhost:9000"
          logs-artifact-name: 'quay-debug'

      - name: Use Quay Initialize API to create the 1st Super User
        run: |
          response=$(curl -X POST ${{ steps.setup-quay.outputs.quay-url }}/api/v1/user/initialize --header 'Content-Type: application/json' \
          --data '{ "username": "admin", "password": "password", "email": "quayqe@redhat.com", "access_token": true }')
          echo "Initialization response: $response"

      - name: Create test results directory
        run: |
          mkdir -p ${{ github.workspace }}/test-results
          mkdir -p ${{ github.workspace }}/cypress-videos
          mkdir -p ${{ github.workspace }}/cypress-screenshots

      - name: Debug - Capture page console with Playwright
        run: |
          npm install playwright
          npx playwright install chromium
          node << 'EOF'
          const { chromium } = require('playwright');
          (async () => {
            const browser = await chromium.launch();
            const page = await browser.newPage();

            // Capture all console messages
            page.on('console', msg => {
              console.log(`[${msg.type().toUpperCase()}]`, msg.text());
            });

            // Capture page errors
            page.on('pageerror', err => {
              console.log('[PAGE ERROR]', err.message);
              console.log(err.stack);
            });

            // Capture failed requests
            page.on('requestfailed', request => {
              console.log('[REQUEST FAILED]', request.url(), request.failure().errorText);
            });

            console.log('Navigating to http://localhost:8080...');
            await page.goto('http://localhost:8080', { waitUntil: 'networkidle', timeout: 60000 });

            console.log('Page title:', await page.title());
            await page.screenshot({ path: 'debug-screenshot.png', fullPage: true });
            console.log('Screenshot saved to debug-screenshot.png');

            await browser.close();
          })();
          EOF

      - name: Upload debug screenshot
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: debug-screenshot
          path: debug-screenshot.png

      # - name: Install Cypress Test Dependencies
      #   run: |
      #     sudo apt-get update && sudo apt-get install -y curl gnupg software-properties-common wget xz-utils git jq python3 python3-pip unzip
      #     sudo apt-get install -y libgtk-3-0 libgbm-dev libgbm1 libnotify-dev libnss3 libxss1 libasound2 libxtst6 xauth xvfb libx11-xcb1 libdbus-glib-1-2 libxt6
      #     sudo ln -sf /usr/bin/python3 /usr/bin/python

      # - name: Install Node.js and Yarn
      #   run: |
      #     curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
      #     sudo apt-get install -y nodejs
      #     sudo npm install --global yarn

      # - name: Install Google Chrome Stable
      #   run: |
      #     sudo wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
      #     sudo apt-get install -y ./google-chrome-stable_current_amd64.deb

      # - name: Install Terraform
      #   run: |
      #     sudo wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      #     echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
      #     sudo apt-get update && sudo apt-get install -y terraform

      # - name: Install ORAS CLI
      #   run: |
      #     VERSION="1.2.2"
      #     curl -LO "https://github.com/oras-project/oras/releases/download/v${VERSION}/oras_${VERSION}_linux_amd64.tar.gz"
      #     sudo tar -zxf oras_${VERSION}_linux_amd64.tar.gz -C /usr/local/bin/ oras

      # - name: Run Cypress tests
      #   run: |
      #     git clone https://github.com/quay/quay-tests.git
      #     QUAY_URL="${{ steps.setup-quay.outputs.quay-url }}"
      #     QUAY_HOSTNAME="${QUAY_URL#http://}"

      #     export CYPRESS_QUAY_ENDPOINT="${QUAY_URL}"
      #     export CYPRESS_QUAY_HOSTNAME="${QUAY_HOSTNAME}"
      #     export CYPRESS_QUAY_USER="admin"
      #     export CYPRESS_QUAY_PASSWORD="password"

      #     echo "Waiting 1 minutes for Quay UI to be fully ready..."
      #     sleep 60

      #     cd quay-tests/quay-api-tests && \
      #     yarn install && \
      #     NO_COLOR=1 yarn run cypress run \
      #     --spec 'cypress/e2e/quay_api_testing_all.cy.js' \
      #     --browser chrome \
      #     --headless \
      #     --reporter cypress-multi-reporters \
      #     --reporter-options configFile=reporter-config.json \
      #     > ${{ github.workspace }}/test-results/quay_api_testing_report

      - name: Run Cypress tests
        run: |
          # Use the Quay URL from setup-quay action output
          QUAY_URL="${{ steps.setup-quay.outputs.quay-url }}"
          # Extract hostname from URL (remove http:// prefix)
          QUAY_HOSTNAME="${QUAY_URL#http://}"

          echo "Quay URL: ${QUAY_URL}"
          echo "Quay Hostname: ${QUAY_HOSTNAME}"

          echo "Waiting 1 minute for Quay UI to be fully ready..."
          sleep 60

          docker run --rm \
            --network host \
            -e CYPRESS_QUAY_ENDPOINT="${QUAY_URL}" \
            -e CYPRESS_QUAY_HOSTNAME="${QUAY_HOSTNAME}" \
            -e CYPRESS_QUAY_USER="admin" \
            -e CYPRESS_QUAY_PASSWORD="password" \
            -v ${{ github.workspace }}/test-results:/test-results \
            -v ${{ github.workspace }}/cypress-videos:/quay-tests/quay-api-tests/cypress/videos \
            -v ${{ github.workspace }}/cypress-screenshots:/quay-tests/quay-api-tests/cypress/screenshots \
            quay.io/quay-qetest/ci-test-image:latest \
            bash -c "
              cd /quay-tests/quay-api-tests && \
              yarn install && \
              NO_COLOR=1 yarn run cypress run \
                --spec 'cypress/e2e/quay_api_testing_all.cy.js' \
                --browser chrome \
                --headless \
                --reporter cypress-multi-reporters \
                --reporter-options configFile=reporter-config.json \
                | tee /test-results/quay_api_testing_report
            "

      - name: Upload Cypress Test Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: quay-api-test-report
          path: ${{ github.workspace }}/test-results/quay_api_testing_report
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload Cypress Videos
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-videos
          path: ${{ github.workspace }}/cypress-videos
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload Cypress Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-screenshots
          path: ${{ github.workspace }}/cypress-screenshots
          retention-days: 30
          if-no-files-found: ignore
