apiVersion: v1
kind: Service
metadata:
  name: prometheus-loadbalancer-service
spec:
  type: LoadBalancer
  ports:
    - name: prometheus-proxy-loadbalancer
      protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: prometheus
